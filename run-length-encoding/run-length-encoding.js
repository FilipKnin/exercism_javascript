export

function encode(notEncryptedString) {
  let encryptedString = [];
  const notEncryptedStringArray = notEncryptedString.split('');
  return notEncryptedStringArray;

  //create variable storing encrypted string
  // convert String into array and store it as a variable
}
